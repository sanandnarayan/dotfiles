# sets the prompt up for git status
source ~/.zsh/zsh_git.sh 
# allows me to use config to store the dot files in a git repo
alias config='/usr/bin/git --git-dir=/Users/anand/.cfg/ --work-tree=/Users/anand'
alias ..='cd ../'
alias ...='cd ../../'
